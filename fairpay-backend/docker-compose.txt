version: '3.8'

services:
  # Banco de dados PostgreSQL
  postgres:
    image: postgres:15-alpine
    container_name: fairpay-postgres
    environment:
      POSTGRES_DB: fairpay
      POSTGRES_USER: fairpay
      POSTGRES_PASSWORD: fairpay123
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - "5433:5432"  # Mudando de 5432 para 5433 no host
    networks:
      - fairpay-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fairpay"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Backend Spring Boot
  backend:
    build:
      context: ./fairpay-backend/fairpay-backend
      dockerfile: Dockerfile
    container_name: fairpay-backend
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/fairpay
      SPRING_DATASOURCE_USERNAME: fairpay
      SPRING_DATASOURCE_PASSWORD: fairpay123
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
      # JWT propriedades (ajuste conforme sua configuração)
      JWT_SECRET: YQXMKYRgH9EHaMFJzdEFODCtYGXNrw/TXkU6vDlxE8Vjt9BlLqJXy1HiO+WjAzAx4YfWQxZcw2pQGL9jVONX7Q==
      JWT_EXPIRATION: 900000
      JWT_REFRESH_EXPIRATION: 86400000
    ports:
      - "8090:8090"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - fairpay-network

  # Frontend Next.js
  frontend:
    build:
      context: ./fairpay-frontend
      dockerfile: Dockerfile
    container_name: fairpay-frontend
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:8090/api
    ports:
      - "3000:3000"
    depends_on:
      - backend
    networks:
      - fairpay-network

networks:
  fairpay-network:
    driver: bridge

volumes:
  postgres-data: